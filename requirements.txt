# --- Tối thiểu: sinh + kiểm định bộ dữ liệu non-IID (GĐ2) ---
numpy>=1.24
pandas>=2.0
Pillow>=9.1
PyYAML>=6.0
matplotlib>=3.7

# --- Kiểm thử ---
pytest>=7.0

# --- Huấn luyện FedAvg + MobileNetV3 (GĐ1/GĐ2/GĐ3) ---
# Tầng dữ liệu KHÔNG cần các gói này: nó chạy thuần numpy/PIL và tự chuyển
# sang torch DataLoader khi phát hiện torch. Cài khi bắt đầu huấn luyện:
#   pip install torch torchvision
#   pip install flwr                    # nền tảng mô phỏng hệ phân tán
# torch>=2.1
# torchvision>=0.16
# flwr>=1.8
