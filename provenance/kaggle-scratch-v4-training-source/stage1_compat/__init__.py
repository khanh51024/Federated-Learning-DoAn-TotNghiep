"""stage1_compat package.

Profile tương thích GĐ1 chỉ huấn luyện FedAvg (alpha=100, alpha=1, seed=42).
Nhập Centralized và Local-only lịch sử dưới dạng reference_only (chỉ đọc).
"""

__version__ = "0.1.0"
